# PostgreSQL connection string
DATABASE_URL="postgresql://user:password@localhost:5432/business_card"

# JWT secret (use a strong value in production)
JWT_SECRET=your-secret-key

# Optional: first super admin (used by seed)
# SUPER_ADMIN_EMAIL=admin@platform.com
# SUPER_ADMIN_PASSWORD=Admin123!
